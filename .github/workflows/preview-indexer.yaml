name: Generate/Updates Preview Index For DA Tenants

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
    inputs:
      site:
        description: "Site name e.g da-bacom"
        required: false
        type: string
      siteRegionPaths:
        description: "Comma-separated regional paths e.g /au/,/de/"
        required: false
        type: string
      lastRunISOFrom:
        description: "Last ISO From timestamp"
        required: false
        type: string
      lastRunISOTo:
        description: "Last ISO To timestamp"
        required: false
        type: string

concurrency:
  group: preview-indexer-${{ github.ref }}
  cancel-in-progress: false

permissions:
  actions: write
  contents: write

env:
  ROLLING_IMPORT_IMS_URL: ${{ secrets.ROLLING_IMPORT_IMS_URL }}
  ROLLING_IMPORT_CLIENT_ID: ${{ secrets.ROLLING_IMPORT_CLIENT_ID }}
  ROLLING_IMPORT_CLIENT_SECRET: ${{ secrets.ROLLING_IMPORT_CLIENT_SECRET }}
  ROLLING_IMPORT_CODE: ${{ secrets.ROLLING_IMPORT_CODE }}
  ROLLING_IMPORT_GRANT_TYPE: ${{ secrets.ROLLING_IMPORT_GRANT_TYPE }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  LINGO_CONFIG: ${{ vars.LINGO_CONFIG }}
  PREVIEW_INDEXER_REPOS: ${{ vars.PREVIEW_INDEXER_REPOS }}
  PREVIEW_INDEX_FILE: ${{ vars.PREVIEW_INDEX_FILE }}
  AEM_ADMIN_TOKEN_ADOBECOM_DA_BACOM: ${{ secrets.AEM_ADMIN_TOKEN_ADOBECOM_DA_BACOM }}
  EXCLUDE_PREVIEW_PATHS_ADOBECOM_DA_BACOM: ${{ vars.EXCLUDE_PREVIEW_PATHS_ADOBECOM_DA_BACOM }}
  PREVIEW_INDEX_KEY_ADOBECOM_DA_BACOM: ${{ vars.PREVIEW_INDEX_KEY_ADOBECOM_DA_BACOM }}
  PREVIEW_PATH_EXTN_ADOBECOM_DA_BACOM: ${{ vars.PREVIEW_PATH_EXTN_ADOBECOM_DA_BACOM }}
  PREVIEW_INDEXER_LOG_LEVEL: ${{ vars.PREVIEW_INDEXER_LOG_LEVEL }}

jobs:
  preview-indexer:
    if: github.repository_owner == 'raga-adbe-gh'
    runs-on: ubuntu-latest
    steps:
      # LOCKING LOGIC BEINGS
      - name: Restore lock cache
        id: cache-restore-lock
        uses: actions/cache@v4
        with:
          path: preview-indexer/lock/indexer.json
          key: preview-indexer-lock-${{ github.run_id }}
          restore-keys: preview-indexer-lock-

      - name: Check and Set Lock
        run: |
          mkdir -p preview-indexer/lock
          
          # Check
          if [ -f preview-indexer/lock/indexer.json ]; then
            echo "Lock file exists. Another indexer is running. Exiting."
            exit 1
          fi
          
          # Create new lock
          echo "locked" > preview-indexer/lock/indexer.json
          echo "Lock set."

      - name: Save lock cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: preview-indexer/lock/indexer.json
          key: preview-indexer-lock-${{ github.run_id }}
      # LOCKING LOGIC ENDS

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Restore cached state
        id: cache-restore
        uses: actions/cache@v4
        with:
          path: preview-indexer/state/
          key: preview-indexer-state-${{ github.run_id }}
          restore-keys: |
            preview-indexer-state-

      - name: Check if lock exists
        run: |
          mkdir -p preview-indexer/state
          if [ -f preview-indexer/state/lock.txt ]; then
            echo "Lock file exists. Another indexer is running. Exiting."
            exit 1
          fi
          echo "No lock found. Proceeding."

      - name: Set lock
        run: |
          echo "locked" > preview-indexer/state/lock.txt

      - name: Initialize state file if missing
        run: |
          if [ ! -f preview-indexer/state/last-runs.json ]; then
            echo "{}" > preview-indexer/state/last-runs.json
          fi

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 21

      - name: Install dependencies
        run: cd ./.github/workflows/preview-indexer/ && npm install

      - name: Run preview-indexer/incremental.js
        run: node ./.github/workflows/preview-indexer/incremental.js
        env:
          LAST_RUN_ISO_TO: ${{ github.event.inputs.lastRunISOTo }}
          LAST_RUN_ISO_FROM: ${{ github.event.inputs.lastRunISOFrom }}
          SITE: ${{ github.event.inputs.site }}
          SITE_REGION_PATHS: ${{ github.event.inputs.siteRegionPaths }}

      - name: Save updated cache
        if: always()
        uses: actions/cache@v4
        with:
          path: preview-indexer/state/last-runs.json
          key: preview-indexer-last-runs-${{ github.run_id }}

      # RELEASING LOCK BEGINS
      - name: Release lock
        if: always()
        run: |
          rm -f preview-indexer/lock/indexer.json

      - name: Save updated cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: preview-indexer/lock/indexer.json
          key: preview-indexer-lock-${{ github.run_id }}
      # RELEASING LOCK ENDS